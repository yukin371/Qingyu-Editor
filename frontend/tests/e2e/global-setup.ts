async function globalSetup() {
  // Reserved for future shared fixtures. Keep this file present because
  // playwright.config.ts wires it as the canonical E2E setup hook.
}

export default globalSetup
