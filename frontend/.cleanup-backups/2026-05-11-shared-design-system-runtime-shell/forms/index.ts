/**
 * 表单组件统一导出
 *
 * @deprecated 仅保留给历史 shared 路径；新代码应优先复用 `src/design-system` 或模块内专有封装。
 */

export { default as FormCard } from './FormCard.vue'
export { default as FormSection } from './FormSection.vue'
